#ifndef PRINT_H
#define PRINT_H



void PrintStartupPhase(char* lastCommand, char* msg); //Prints the terminalwindow for startupPhase

void PrintPlayPhase(char* lastCommand, char* msg); //Prints the terminalwindow for playPhase

#endif //PRINT_H
